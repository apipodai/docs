---
title: "Seedance 2.0 Fast 参考生视频"
description: "Seedance 2.0 Fast 参考生视频是低延迟路由，使用参考图片、视频或音频引导主体、动作、视觉风格和声音。Seedance 系列强调提示词遵循、连贯运动、多镜头叙事和音画同步。 素材默认（real_person=true）先送审，确认不含真人时可设置 real_person=false 跳过送审、直接透传。"
openapi: "api-reference/openapi/seedance--2-0-fast-reference-to-video.zh.yaml POST /v1/videos/generations"
---

<Warning title="条件校验">必须至少提供一种参考素材；图片、视频和音频的数量上限以 API 参数定义为准。</Warning>

## 素材与场景限制

Seedance 2.0 Fast 参考素材模式，支持 9 张参考图片、3 个参考视频、3 个音频素材。满血版本，支持真人、人脸；素材默认送审（real_person=true），确认不含真人时可设置 real_person=false 跳过送审、直接透传。

- **文本提示词**：中文建议不超过 500 字，英文建议不超过 1000 词。
- **图片输入**：支持 jpeg、png、webp、bmp、tiff、gif、heic、heif；单张图片小于 30 MB，宽高比 (0.4, 2.5)，宽高长度 (300px, 6000px)。
- **多模态参考场景**：不超过 9 张。
- **视频输入**：支持 mp4、mov；单个视频时长 [2, 15] 秒，最多 3 个参考视频，所有视频总时长不超过 15 秒；单个视频小于 50 MB；帧率 [24, 60]。
- **音频输入**：支持 wav、mp3；单段音频时长 [2, 15] 秒，最多 3 段参考音频，所有音频总时长不超过 15 秒；单段音频小于 15 MB。
- **音频输入限制**：音频不可单独输入，至少需要同时包含 1 个参考图片或视频。

<Note>
🔸注意🔸：带视频输入的单价更低，因其计算方式不同：无视频 = 单价 × 输出量；带视频 = 单价 ×（输入量 + 输出量）。
</Note>

## 真人素材送审（real_person）

请求携带参考素材时，通过 `real_person` 布尔字段控制素材是否先经人脸素材送审：

- `real_person: true`（默认）：素材先经素材服务送审，通过后提交任务；送审期间任务等待，审核拒绝时报错。
- `real_person: false`：跳过送审，素材 URL 直接透传提交任务。

仅在确认素材不含真人/人脸时才设置为 `false` 以省去送审等待；含真人素材保持默认 `true` 即可。
