---
title: "Seedance 2.0 特价版 参考生视频"
description: "Seedance 2.0 Lite 参考生视频是特价版满血路由，基于图片、视频与音频参考素材生成视频。Seedance 系列强调提示词遵循、连贯运动、多镜头叙事和音画同步。该模型为特价版满血路由：真人素材默认（real_person=true）在提交生成前会经素材服务送审；确认素材不含真人时可设置 real_person=false 跳过送审、直接透传，请求中直接传入公开 URL 即可。"
openapi: "api-reference/openapi/seedance--2-0-lite-reference-to-video.zh.yaml POST /v1/videos/generations"
---

## 真人素材送审（real_person）

请求携带参考素材时，通过 `real_person` 布尔字段控制素材是否先经人脸素材送审：

- `real_person: true`（默认）：素材先经素材服务送审，通过后提交任务；送审期间任务等待，审核拒绝时报错。
- `real_person: false`：跳过送审，素材 URL 直接透传提交任务。

仅在确认素材不含真人/人脸时才设置为 `false` 以省去送审等待；含真人素材保持默认 `true` 即可。
