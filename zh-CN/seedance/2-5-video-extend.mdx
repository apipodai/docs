---
title: "Seedance 2.5 视频延长"
description: "Seedance 2.5 视频延长将全模态参考子任务固定为视频延长：传入源视频并描述向前或向后延续，输出 4-30 秒，画幅自动跟随源视频（不可设置）。"
openapi: "api-reference/openapi/seedance--2-5-video-extend.zh.yaml POST /v1/videos/generations"
---

<Warning title="条件校验">必须至少提供一条参考视频（待延长的源视频）。</Warning>

## 延长限制

Seedance 2.5 视频延长是官方折扣路由全模态参考家族的视频延长子任务。传入源视频和明确的延续措辞后，平台会在上游固定 `omni_reference_task_type=extend`，子任务参数限制在提交时同步校验：参数不兼容立即返回 400，而不是等任务跑几分钟后异步失败。

- **源视频**：必须至少一条参考视频。
- **时长**：4-30 秒（默认 4），对应延长后的输出时长。
- **画幅**：不可设置。输出画幅自动保持与源视频一致；契约不暴露该字段，传入任意值都会被忽略（固定按 adaptive 提交）。
- **参考素材**：最多 30 张图、10 条视频、10 段音频；音频不能单独上传。
- **提示词写法**：使用「向前延长 / 向后延长 / 续写」等明确的延续措辞；歧义的延长或编辑意图仍可能被模型重新判定。

<Note>
🔸注意🔸：启用带视频输入时，因计费计算公式不同，单价更低：无视频 = 单价 × 输出时长；带视频 = 单价 ×（输入时长 + 输出时长）。
</Note>

想在原片上就地编辑而不是延长？请使用 [Seedance 2.5 视频编辑](/zh-CN/seedance/2-5-video-edit)。
