---
title: "Seedance 2.5 图生视频"
description: "Seedance 2.5 图生视频是官方满血折扣路由，将首帧图（可选尾帧图）动画化为视频。Seedance 系列强调提示词遵循、连贯运动、多镜头叙事和音画同步。该系列为满血版：媒体 URL 默认（real_person=true）会先经素材服务送审再提交任务；确认素材不含真人时可设置 real_person=false 跳过送审、直接透传，公网 URL 均可直接使用。"
openapi: "api-reference/openapi/seedance--2-5-image-to-video.zh.yaml POST /v1/videos/generations"
---

## 真人素材送审（real_person）

请求携带参考素材时，通过 `real_person` 布尔字段控制素材是否先经人脸素材送审：

- `real_person: true`（默认）：素材先经素材服务送审，通过后以 `assetId://{assetId}` 引用提交任务；送审期间任务等待，审核拒绝时报错。
- `real_person: false`：跳过送审，素材 URL 直接透传提交任务。

仅在确认素材不含真人/人脸时才设置为 `false` 以省去送审等待；含真人素材保持默认 `true` 即可。

## 素材库引用

除可公开访问的 URL 外，`image_url` / `image_urls`、`video_urls`、`audio_url` / `audio_urls` 字段还支持素材库引用，格式为 `assetId://{assetId}`。素材 ID 为全平台共享标识：任何账号都可以引用已知的 `assetId`，不限提交者，与公开 URL 的共享语义一致。仅 `ACTIVE` 状态的素材可通过校验。素材的提交与状态查询见[素材提交](/zh-CN/seedance/asset-upload)与[素材详情](/zh-CN/seedance/asset-detail)。
