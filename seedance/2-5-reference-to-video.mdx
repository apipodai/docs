---
title: "Seedance 2.5 Reference to Video"
description: "Seedance 2.5 Reference to Video is the official full-capability discount route that uses reference images, video, or audio to guide subjects, motion, visual style, and sound. The Seedance family emphasizes prompt following, coherent motion, multi-shot storytelling, and synchronized audio. This is a full-capability variant where media URLs are reviewed by the asset service before the task is submitted, so plain public URLs still work in requests."
openapi: "api-reference/openapi/seedance--2-5-reference-to-video.yaml POST /v1/videos/generations"
---

<Warning title="Conditional validation">Provide at least one reference asset. The per-media limits are defined by the API schema.</Warning>

## Asset and Scene Limits

Seedance 2.5 reference-material mode is a full-capability route with a built-in asset library, no facial distortion, a 100% pass rate for real-person content, and long-term stability. Pricing is approximately 10% off the official price.

- **Text prompt**: For Chinese, keep it under 500 characters; for English, under 1,000 words.
- **Image input**: Supports jpeg, png, webp, bmp, tiff, gif, heic, and heif formats; each image under 30 MB; aspect ratio range (0.4, 2.5); width and height range (300px, 6000px).
- **Image count**: No more than 30 images.
- **Video input**: Each video 2–30 seconds; up to 10 reference videos; total video duration must not exceed 30 seconds.
- **Audio input**: Each clip 2–30 seconds; up to 10 reference audio clips; total audio duration must not exceed 30 seconds.
- **Audio input restriction**: Audio cannot be provided on its own; at least one reference image or reference video is required.

<Note>
Note: When video input is enabled, the unit price is lower because the billing formula differs: without video = unit price × output duration; with video = unit price × (input duration + output duration).
</Note>

## Asset library references

Besides publicly accessible URLs, the `image_url` / `image_urls`, `video_urls`, and `audio_url` / `audio_urls` fields also accept asset library references in the form `assetId://{assetId}`. Asset IDs are shared across the platform: any account may reference a known `assetId`, regardless of who submitted it — the same sharing semantics as a public URL. Only `ACTIVE` assets pass validation. Submit and track assets via [asset upload](/seedance/asset-upload) and [asset detail](/seedance/asset-detail).
